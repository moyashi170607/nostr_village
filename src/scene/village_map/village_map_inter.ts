export interface InfoBoxUI {
    visible: boolean
}

export interface CameraMoveUI {
    visible: boolean,
    active: boolean,
    speed: number
}

export interface Building {
    gridX: number
    gridY: number
}